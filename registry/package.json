{
  "name": "@pkg-boilerplate/registry",
  "version": "0.0.0",
  "private": true,
  "description": "Source items and build script that compile to a static JSON registry.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsx build.ts",
    "dev": "tsx watch build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pkg-boilerplate/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
