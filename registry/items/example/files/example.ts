/**
 * Example registry item.
 *
 * Everything in this file is copied verbatim into the consumer's project when
 * they run `add example`. Replace it with whatever you want to distribute:
 * a React component, an MDX block, a utility, a config, etc.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
