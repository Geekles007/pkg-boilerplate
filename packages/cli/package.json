{
  "name": "@pkg-boilerplate/cli",
  "version": "0.0.0",
  "description": "CLI to add registry items into a project.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "pkg-boilerplate": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pkg-boilerplate/core": "workspace:*",
    "commander": "^12.1.0",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
