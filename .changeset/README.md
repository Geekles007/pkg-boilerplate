# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a version bump for `@pkg-boilerplate/core` and
`@pkg-boilerplate/cli`, then `pnpm version-packages` and `pnpm release` to
publish.
